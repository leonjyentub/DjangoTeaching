[project]
name = "learnboard"
version = "0.1.0"
description = "A teaching-oriented Django message board project"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "django>=6.1.1,<6.2",
]

[dependency-groups]
dev = [
    "coverage>=7.6",
]

[tool.uv]
package = false
