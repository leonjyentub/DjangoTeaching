[project]
name = "learnmart"
version = "0.1.0"
description = "A teaching-oriented Django marketplace project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2,<5.3",
    "pillow>=11.0",
]

[dependency-groups]
dev = [
    "coverage>=7.6",
]

[tool.uv]
package = false

